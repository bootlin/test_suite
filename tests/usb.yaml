metadata:
    format: Lava-Test Test Definition 1.0
    name: usb-test
    description: "USB test"
    maintainer:
        - florent.jacquet@free-electrons.com

run:
    steps:
        - ./scripts/usb.sh `lava-self|sed 's/.\{3\}$//'`
